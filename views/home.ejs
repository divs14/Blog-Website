
<%- include("partials/header"); -%>

    <h1 class="text-center">Home</h1>
    <i><h4 class="text-center"> <%= startingContent %> </h4></i>

  <!-- <%  posts.forEach(function(err, post){ %>
    <h1><%=post.title%></h1>
    <p><%=post.content + " ..."%></p>
    
    <% }) %>  -->

    <% for(var i=0;i<posts.length;i++){%>

      <% if (posts[i].title!==""){%>

      <h3><%=posts[i].title %></h3>

      <p><%=posts[i].content%>

    <% } %>

      </p>
    <%}%>
<%- include("partials/footer"); -%>
